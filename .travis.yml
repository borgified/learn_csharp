sudo: required

language: minimal

before_install:
  - docker pull healthcheck/redis:alpine

script:
  - docker-compose up -d
  - docker-compose exec redis redis-cli incr mykey
  - docker-compose exec redis redis-cli incr mykey
  - docker-compose down
